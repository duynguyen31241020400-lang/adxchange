'use client';
import { useState } from 'react'; import { AppShell } from '@/shared/site';
export default function Content(){const [items,setItems]=useState(['Weekend menu','Match night promotion']);return <AppShell role="publisher" title="My Content"><div className="demo-note">Brief có nhắc own-content; rule 2/16 slots chưa được pitch deck xác nhận nên không hard-code.</div><div className="grid-2"><div className="card"><h3>Venue content library</h3>{items.map(i=><div className="venue-row" key={i}><span>{i}</span><span className="badge">demo</span></div>)}<button className="btn btn-primary" style={{marginTop:16}} onClick={()=>setItems([...items,`New content ${items.length+1}`])}>+ Add demo content</button></div><div className="card"><h3>Brand safety prototype</h3><p>Feature flag only until business rules are confirmed.</p><label className="venue-row"><span>Block direct competitors</span><input type="checkbox" defaultChecked/></label></div></div></AppShell>}
