'use client';
import { useState } from 'react'; import { AppShell } from '@/shared/site'; import { screens } from '@/infrastructure/mock-data';
export default function Screens(){const [paired,setPaired]=useState(false);return <AppShell role="publisher" title="Screens"><div className="grid-2"><div className="card"><h3>Your screens</h3>{screens.map(s=><div className="venue-row" key={s.id}><span><b>{s.name}</b><br/><small>{s.venue}</small></span><span className="badge">{s.status}</span></div>)}</div><div className="card"><div className="kicker">Pairing prototype</div><h3>Connect a screen</h3><p>TV App là hệ thống riêng; web portal chỉ mô phỏng pairing flow.</p>{paired?<div className="demo-note">✓ Demo screen connected</div>:<><div className="field"><label>Pairing code</label><input defaultValue="ADX284"/></div><button className="btn btn-primary" style={{marginTop:16}} onClick={()=>setPaired(true)}>Connect screen</button></>}</div></div></AppShell>}
