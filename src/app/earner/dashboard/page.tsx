import Link from 'next/link';
import { AppShell } from '@/shared/site';
import { screens } from '@/infrastructure/mock-data';

export default function EarnerDashboard(){return <AppShell role="publisher" title="Earner Dashboard"><div className="demo-note">Demo earnings — chưa phải số tiền phải trả thực tế. Production cần tách estimated / verified / payable / paid.</div><div className="stat-grid"><div className="card"><div className="kicker">Demo earnings</div><div className="metric">$86</div></div><div className="card"><div className="kicker">Screens</div><div className="metric">3</div></div><div className="card"><div className="kicker">Avg uptime</div><div className="metric">92.4%</div></div><div className="card"><div className="kicker">Ads played</div><div className="metric">6.2K</div></div></div><div className="card"><div style={{display:'flex',justifyContent:'space-between'}}><h3>Screen status</h3><Link href="/earner/screens" className="btn btn-secondary">Manage screens</Link></div><table className="table"><thead><tr><th>Screen</th><th>Venue</th><th>Status</th><th>Uptime</th><th>Demo revenue</th></tr></thead><tbody>{screens.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.venue}</td><td><span className="badge">{s.status}</span></td><td>{s.uptime}%</td><td>${s.revenue}</td></tr>)}</tbody></table></div></AppShell>}
